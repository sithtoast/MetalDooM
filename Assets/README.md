# Placeholder app icon

`AppIcon.png` is the original generated source, with transparency. The app build uses `scripts/build-icon.sh` to create the standard macOS icon sizes and package them as `AppIcon.icns`.

Generated with the built-in image generation tool.

Prompt:

Use case: logo-brand. Asset type: macOS app icon for MetalDooM, a classic Doom source port rendered with Metal. Create one polished placeholder icon, square 1024x1024. A bold sculpted metallic capital M, with heavy angular beveled industrial geometry reminiscent of classic 1990s shooter title lettering, brushed gunmetal and warm bronze highlights, subtle fiery orange light from below. The M is the only letter and fills most of a dark charcoal rounded-square tile. Strong simple silhouette readable at small Dock sizes. Front-facing, centered, restrained texture, crisp edges, premium native macOS icon feel. Tile inset roughly 8 percent within the canvas, genuinely transparent background outside the rounded tile, subtle shadow. No extra text, no mockup, no scene, no weapon, no watermark. Render the finished icon asset only.
